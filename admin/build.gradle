dependencies {
    implementation project(":tincase-core")

    implementation "$boot:spring-boot-starter-validation"
    implementation "$boot:spring-boot-starter-web"
    implementation "$boot:spring-boot-starter-security"
}